name: Point Expiration Disabled

on: []

jobs:
  point-expiration-disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - name: Explain the production safety gate
        run: echo "Point expiration is disabled pending FIFO allocation, migration, and reconciliation approval."
