# 2026年9月5日 キャスト・出勤表の再修正

対象: [#282 キャスト](https://app.notion.com/p/3ccdda8d8bde80b19a27f2cd7dd8a2d1)、[#283 出勤表](https://app.notion.com/p/3ccdda8d8bde803fa9b6eeddcb522958)。ユーザーが追加した指摘を含む。反映先は https://salon.c-platinum.com/admin/ を提供する `salon-preview-system` のみ。

## 修正内容

- 新規キャストはフォーム・APIとも仮登録。週間表は在籍だけを集計・表示。キャスト一覧は在籍を初期表示し、仮登録・在籍・退店の3状態で絞り込む。
- 一覧の操作部・カード画像・週間表の行を縮める。本人確認の警告は「身分証 未確認」「住民票 未確認」を一覧に表示する。
- 手取りUPをお客様向け指名料と別の金額マスタへ分離。管理 > 設定 > 指名料金設定 > キャスト手取UPで、フリー・パネル・おすすめP・本指名を個別に追加・編集・停止できる。キャストの基本情報からそれぞれ選択する。
- 既存の手取りUP選択は同額の内部マスタへ移行し、お客様の料金・分配額とキャストのその他情報を保持。オーダー入力中の計算、予約保存時の計算の双方で4種類を個別に適用する。手取りUPをお客様へ追加請求しない。
- ネット予約の本指名料金は店舗の本指名マスタを参照する。未設定のキャストの本指名が0円になる不具合を解消。店舗に料金が未登録なら予約を受け付けずエラーを表示する。
- テンプレート保存と個別キャスト概要からの出勤保存に店舗IDを明示。保存エラー時は入力を保持し、読込失敗時は既存テンプレートの上書きを防ぐ。時間の二重表記を除去。
- 出勤保存を待ち、成功後も編集画面を開いたままにする。変更した日だけ保存し、未入力へ戻す操作も反映。媒体用テキストと備考を別々に保存する。
- 編集ヘッダーにキャスト名・保存・前週/翌週を固定し、下部にも移動・保存・閉じるを配置。編集の途中で閉じる場合とブラウザの再読込・離脱時は未保存警告を表示する。
- 4週間は日付を選んで1日ずつ入力するカレンダー形式へ変更。保存済みテンプレートを選んで日付をクリックすると一括入力できる。週移動時・親画面の再取得時も未保存内容を保持し、キャスト切替では分離する。

- 本番UIの新規入力確認でフォーム側の在籍初期値と、生年月日の途中選択が消える不具合を追加検出。新規の仮登録送信、年・月・日の選択保持、不完全日付の保存防止、誕生日前後を考慮した年齢計算まで回帰テストを追加した。

- 保存後の翌週再表示で、日付だけを渡す読込APIがUTC基準となり日本時間の週初日を除外する問題を検出。日付指定を日本時間の00:00〜23:59:59.999へ変更し、予約表から渡す明示的な時刻範囲は保持する。

- 実機1280×720の確認に基づき、グリッド/リスト切替を検索・追加と同じツールバーへ移動。カードの画像・警告・余白と週間表の行をさらに圧縮。低リスクのレイアウト変更は既存の回帰テストと実画面の寸法で検証する。

## 旧データと既存の対応

キャストのDB件数は222件。途中のAPI全件照合で同名データのページ境界に重複を検出したため、IDを含む確定順序へ修正した。状態別の最終件数はDBとの照合結果を採用する。旧画面の221件の追加取込と画像移行は同日の先行作業で完了済み。現在の手修正を古いスナップショットで戻さないため、今回は再インポートしない。

電話・生年月日・ブログ・SNS・入退店日・面接担当者・求人媒体・本人確認日は先行取込済みの値を保持する。元データが未入力の項目は補造しない。旧パスワードや本人確認書類の画像はプロフィール移行の対象外。

基本情報/公開プロフィールのタブ分離、重複紹介文の削除、タイプの詳細欄への移動、画像とオプションの枠分け、可能プレイ4列、◯公開/△管理画面のみ/×不可、電話・チャット導線、タイムラインの名前横ランク表示は先行実装を含めて再確認する。

## 検証

- 不具合を再現するテストを先に追加してRedを確認後に修正した。
- `./scripts/ci.sh`: 整形・lint・型検査・全2,996テスト（395ファイル）・既存のカバレッジ閾値・Playwright 2件・本番ビルド。カバレッジ実測は行73.38%、分岐80.88%であり100%とは扱わない。
- 実データの暗号化バックアップを隔離PostgreSQLへ復元し、手取りマスタ移行を実行。222キャスト・2,181予約を保持。既存の手取り額・店舗所属・お客様料金・手取り設定以外のキャスト情報が変わらないことをSQLで照合した。
- Heaven管理画面の月間/日付クリック後の時間入力を閲覧して参考にした。Heaven上の情報は変更していない。
- Geminiによる追加レビューは有用な回答が得られなかったため、承認済みとは扱わない。保存経路と実データ移行はコード・テスト・隔離DBで確認した。

## 反映結果（2026-09-05 22:49 JST）

- VPS稼働版: `b16b2b25f7a9f202dd747a101725fe0b9890c76b`。イメージ: `sha256:030ef17518f0aa64027c9a52efdfcc2587dcace4c61c979056a88de95a9ff1b2`。`/api/health` はready、DB/storageもready。ほかのサービスのコンテナIDは変わっていない。
- 直前の暗号化バックアップ: `/opt/platinum/maintenance/salon-preview-deploy-20260905T134435Z`。復元照合済み。ロールバック: `/opt/salon-preview-rollbacks/20260905T134919Z-cf64948e078b7e857e7818f1a71934e421cd68c6`。
- 最終DB件数: キャスト222（在籍35・仮登録1・退店186）、顧客13,316、予約2,181、適用済みmigration29。
- 最初の反映前の暗号化DBバックアップを隔離DBへ復元し、既存222キャストの全列（意図した4つの手取りUP参照を除く）のハッシュが一致。欠損・追加・その他変更は0。お客様向け指名料8件も、意図したおすすめ分類修正と新フラグを除く全列のハッシュが一致した。
- 管理者APIの全ページも222件・222ユニークID。途中の34在籍/187退店というAPI集計はページ境界で1件重複していたため、最終DBと一致する35在籍/186退店へ訂正した。旧データを再取込して状態を変えたものではない。
- 手取りUPは4種類×1,000/2,000/3,000円の12選択肢を用意。既存の同額選択1件は保持し、不足11件を追加した。既存キャストへ新しい金額を自動選択していない。お客様向け一覧は8件のまま。未認証の内部マスタ取得はHTTP401で拒否。

## ログイン後の実操作

- 確認用キャストを2件新規保存。最終版では初期の仮登録を変えずに追加でき、在籍の一覧・週間表からは除外される。
- 電話、ブログ、SNS、入店日、本人確認日を保存して再取得。本人確認2件の警告が概要から消えることを確認。生年月日は途中選択を保持し、1999-12-31を選ぶと26歳として保存される。
- フリー1,000円・パネル2,000円・おすすめP3,000円・本指名1,000円をそれぞれ選んで保存し、参照先の金額が一致。管理画面からフリー3,000円を2,500円へ変更し再読込でも保持されることを確認後、3,000円へ戻した。
- 10:00〜14:00のテンプレートを保存して閉じ、再表示。時間の重複表記なし。4週間カレンダーの日付クリックで9/7へ適用。媒体用テキストと店舗内備考を別々に保存した。
- 保存完了後も編集画面が開いている。閉じて翌週を表示すると月曜日の時間・媒体文・備考が復元される。未入力へ戻して下部の保存ボタンで保存し、読込APIでも状態・媒体文・備考を確認した。
- 未保存の備考変更で閉じると確認ダイアログが出ることを確認。確認用の変更だけ破棄した。
- 最終のキャスト一覧は995×845で2段が完全表示（2段目の最下部691px）。週間表は1280×720で6名が完全表示（6名目の最下部691px）。
- 退店フィルターで186件を表示。公開プロフィールの画像/可能オプションの枠、◯/△/×の説明、基本情報タブの項目を確認した。
- 実在2キャストの予約表で、源氏名と「ランク：ブロンズ」の上端が一致し、金額が表示されないことをDOMで確認した。
- 確認用2キャストとその出勤データは削除済み。実在キャストのプロフィールや予約に確認用の変更を残していない。
- Notion #282/#283へ簡単な対応コメントを追加し、「本番環境反映済」への更新を再取得で確認。#281設定の先行反映も最新版に含まれる。

最終CIは2,996テスト/395ファイル、Playwright2件、整形/lint/型検査/カバレッジ閾値/本番ビルドが通過。`DOC_CHECKLIST.md` は生成されていない。旧管理画面そのものからの正式切替や外部媒体への自動投稿を実施したとは扱わない。
